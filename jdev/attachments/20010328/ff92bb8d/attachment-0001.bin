
On Wednesday, March 28, 2001, at 10:44 AM, Bill Ataras wrote:


<excerpt>So is the libfaim cvs at sourceforge (linked to by the
libfaim homepage) the

most current or is Gaim's version of faim current?

</excerpt>

Some of the fixes are not in the faim library itself but in the way
the library is called. For example, the client info struct I mentioned
previously is passed into the faim login call, so the changes to work
around the block are actually in the client code that sets up the
struct.<color><param>0000,0000,DEB7</param>

</color>

--Jens