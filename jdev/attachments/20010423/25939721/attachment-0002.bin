
On Monday, April 23, 2001, at 02:40 PM, Todd Bradley wrote:


<excerpt>Yes, but SMTP servers have never pretended to be

"instant".  If I'm running an INSTANT messaging

server (which I am), I want the traffic to be as 

lightweight as reasonable so that processing and

delivery remains as INSTANT as possible.

</excerpt>

Well, I'm still waiting for evidence that file transfer is going to
put excessive load on a Jabber server. But even so, the server could
use simple quality-of-service techniques like lowering the priority of
file-transfer IQ result elements to preserve the low latency of
regular presence and messaging.<color><param>0000,0000,DEB7</param>

</color>

<fontfamily><param>Marker Felt</param><color><param>0000,5151,0101</param><bigger><bigger><bigger><bigger>—Jens</bigger></bigger></bigger></bigger></color></fontfamily>